const intup = [9, 19, 1, 6, 0, 5, 4];
const numbersInArray = 7;
let arrayOfNumbers = intup;

while (numbersInArray >= 2020) {
    //if last number ==firsttimespoken -> newNumber ==0;if( firsttimespoken(0))turnsappart
}
function firstTimeSPoken(number, arrayOfNumbers) {
    const countOccurrences = (arrayOfNumbers, number) => arr.reduce((a, v) => (v === val ? a + 1 : a), 0);
    return Boolean;
}

function TurnsAppart(number) {
    return Number;
}
